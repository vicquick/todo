
  # todolist

  This is a code bundle for todolist. The original project is available at https://www.figma.com/design/kl0d0NxOO8YC4JbYo20yXN/todolist.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  